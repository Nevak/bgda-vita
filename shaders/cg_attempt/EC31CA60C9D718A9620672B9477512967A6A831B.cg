// Cg Fragment Shader

sampler2D u0;  // our texture (uniform sampler2D in GLSL)

float4 main(
    float2 oT0 : TEXCOORD0  // This corresponds to "varying vec2 oT0" in GLSL
) : COLOR
{
    // Same as: gl_FragColor = tex2D(u0, oT0);
    return tex2D(u0, oT0);
}